- we use **pwd** to find the name of the current directory
- **ls** is used to list files and directories
- **ls -a** will show hidden files also
- **ls -l** provides a long listing providing many details about your files
- **ls -l /directory** you'll see the properties of that directory
- **ls -lrt** shows a time-sorted list of files
- 