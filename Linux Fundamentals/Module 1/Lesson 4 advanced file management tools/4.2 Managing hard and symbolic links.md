- **ln** creates a new link 
- **ln file newfile** creates a hard link between a file and a new file
- **ls -il** shows the inode number
- **ln -s** creates a symbolic link 

In hard links there's no difference between the original file and the file that you linked later