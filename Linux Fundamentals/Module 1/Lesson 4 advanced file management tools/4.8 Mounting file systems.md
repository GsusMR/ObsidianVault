Mounts in Linux are very important, you'll see it knowing that Linux has different partitions. And these partitions are mounted on different directories

- **lsblk** lists block devicesin any mount starts with a block device that is connected to sem directory
- **mount** lists al current mounts
- **df -h** presents mounted devices, including available disk space
- **findmnt** shows all mounts
